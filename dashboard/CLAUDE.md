# Argus Dashboard

Next.js 기반 AI 코딩 에이전트 모니터링 대시보드. 인증 없이 로컬에서 실행한다.

## 기술 스택

- **프레임워크**: Next.js 15+ (App Router)
- **언어**: TypeScript (strict mode)
- **스타일**: Tailwind CSS + shadcn/ui
- **차트**: Recharts
- **DB**: SQLite (`better-sqlite3`, WAL 모드)
- **패키지 매니저**: pnpm

## 프로젝트 구조

```
src/
├── app/
│   ├── layout.tsx                # 루트 레이아웃 (Nav 포함)
│   ├── page.tsx                  # Overview (오늘의 요약)
│   ├── daily/page.tsx            # 일별 추이 (30일)
│   ├── sessions/page.tsx         # 세션 목록
│   ├── tools/page.tsx            # 도구 상세 분석 (Built-in/Orchestration/MCP)
│   ├── efficiency/page.tsx       # 효율성 분석 (M3)
│   ├── config-history/page.tsx   # 설정 변경 이력 (M4)
│   └── api/
│       ├── health/route.ts
│       ├── ingest/route.ts              # POST (OTLP JSON 수신)
│       ├── ingest/tool-detail/route.ts # POST (개별 도구 호출 추적)
│       ├── seed/route.ts         # POST (테스트 데이터 시드)
│       ├── overview/route.ts     # GET ?agent_type=all
│       ├── daily/route.ts        # GET ?days=30&agent_type=all
│       ├── sessions/route.ts     # GET ?agent_type=all
│       ├── models/route.ts       # GET ?agent_type=all
│       ├── efficiency/route.ts   # GET ?days=7
│       ├── tools/route.ts        # GET ?days=7&agent_type=all&detail=true
│       └── config-history/route.ts  # GET ?days=30
├── lib/
│   ├── db.ts                     # SQLite 클라이언트 + 스키마 초기화
│   ├── queries.ts                # SQLite 쿼리 함수
│   ├── agents.ts                 # 에이전트 정의 (색상, 아이콘, 추적 파일 목록)
│   ├── efficiency.ts             # 효율성 점수 계산
│   └── config-tracker.ts         # Git diff 기반 설정 변경 추적
└── components/
    ├── ui/                       # shadcn/ui 컴포넌트
    ├── nav.tsx                   # 사이드바 네비게이션
    ├── stats-card.tsx            # 통계 카드
    ├── agent-filter.tsx          # 에이전트 필터 탭
    ├── cost-chart.tsx            # 비용 추이 차트
    ├── token-chart.tsx           # 토큰 추이 차트
    ├── model-pie-chart.tsx       # 모델별 비율
    ├── agent-comparison.tsx      # 에이전트별 비용 비교
    ├── tool-usage-chart.tsx      # 도구 사용 빈도 차트 (Overview용)
    ├── tool-detail-table.tsx     # 도구 상세 테이블 (카테고리별)
    ├── daily-tool-chart.tsx      # 일별 도구 사용 스택 차트
    ├── individual-tool-table.tsx  # 개별 스킬/에이전트/MCP 테이블
    └── config-timeline.tsx       # 설정 변경 타임라인
```

## 코딩 규칙

### 페이지

- App Router 사용, 기본적으로 Server Component
- 클라이언트 인터랙션 필요 시에만 `'use client'` 추가
- 페이지는 `export default function` 사용 (Next.js 규칙)
- 데이터 페칭은 API 라우트 호출 또는 서버 컴포넌트에서 직접

### API 라우트

```typescript
// src/app/api/{name}/route.ts 패턴
import { NextRequest, NextResponse } from 'next/server'
import { getOverviewStats } from '@/lib/queries'

export async function GET(request: NextRequest) {
  const searchParams = request.nextUrl.searchParams
  const agentType = searchParams.get('agent_type') || 'all'

  // SQLite 쿼리
  // NextResponse.json() 반환
}
```

### SQLite 쿼리

```typescript
// src/lib/queries.ts 패턴
import { getDb } from './db'

export const getOverviewStats = async (agentType: string) => {
  const db = getDb()
  const filter = agentType !== 'all' ? 'AND agent_type = ?' : ''
  const params = agentType !== 'all' ? [agentType] : []
  return db.prepare(`SELECT ... FROM agent_logs WHERE ... ${filter}`).get(...params)
}
```

### 컴포넌트

- Arrow function + Named export
- Props는 `type` 키워드로 정의
- shadcn/ui 컴포넌트는 `src/components/ui/`에 위치
- 차트: Recharts (`ResponsiveContainer`, `LineChart`, `BarChart`, `PieChart`)

### 에이전트 필터

모든 데이터 페이지에 에이전트 필터가 포함된다:
- `all`: 전체
- `codex`: Codex CLI
- `claude`: Claude Code
- `gemini`: Gemini CLI

쿼리 파라미터 `agent_type`으로 필터링한다.

## 환경 변수

```
ARGUS_DB_PATH=../argus.db  # SQLite 경로 (기본값: ../argus.db)
```

## 에이전트 색상 테마

| 에이전트 | 메인 색상 | Tailwind |
|----------|----------|----------|
| Codex | Green | `emerald-500` |
| Claude | Orange | `orange-500` |
| Gemini | Blue | `blue-500` |
| All | Purple | `violet-500` |


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

*No recent activity*
</claude-mem-context>